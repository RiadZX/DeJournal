/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { DAILY_JOURNAL_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const EDIT_ENTRY_DISCRIMINATOR = new Uint8Array([
  229, 145, 96, 144, 117, 131, 101, 193,
]);

export function getEditEntryDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EDIT_ENTRY_DISCRIMINATOR);
}

export type EditEntryInstruction<
  TProgram extends string = typeof DAILY_JOURNAL_PROGRAM_ADDRESS,
  TAccountEntry extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEntry extends string
        ? WritableAccount<TAccountEntry>
        : TAccountEntry,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type EditEntryInstructionData = {
  discriminator: ReadonlyUint8Array;
  mood: string;
  weather: string;
  message: string;
};

export type EditEntryInstructionDataArgs = {
  mood: string;
  weather: string;
  message: string;
};

export function getEditEntryInstructionDataEncoder(): Encoder<EditEntryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['mood', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['weather', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['message', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: EDIT_ENTRY_DISCRIMINATOR })
  );
}

export function getEditEntryInstructionDataDecoder(): Decoder<EditEntryInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['mood', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['weather', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['message', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getEditEntryInstructionDataCodec(): Codec<
  EditEntryInstructionDataArgs,
  EditEntryInstructionData
> {
  return combineCodec(
    getEditEntryInstructionDataEncoder(),
    getEditEntryInstructionDataDecoder()
  );
}

export type EditEntryInput<
  TAccountEntry extends string = string,
  TAccountAuthority extends string = string,
> = {
  entry: Address<TAccountEntry>;
  authority: TransactionSigner<TAccountAuthority>;
  mood: EditEntryInstructionDataArgs['mood'];
  weather: EditEntryInstructionDataArgs['weather'];
  message: EditEntryInstructionDataArgs['message'];
};

export function getEditEntryInstruction<
  TAccountEntry extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof DAILY_JOURNAL_PROGRAM_ADDRESS,
>(
  input: EditEntryInput<TAccountEntry, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): EditEntryInstruction<TProgramAddress, TAccountEntry, TAccountAuthority> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DAILY_JOURNAL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    entry: { value: input.entry ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.entry),
      getAccountMeta(accounts.authority),
    ],
    programAddress,
    data: getEditEntryInstructionDataEncoder().encode(
      args as EditEntryInstructionDataArgs
    ),
  } as EditEntryInstruction<TProgramAddress, TAccountEntry, TAccountAuthority>;

  return instruction;
}

export type ParsedEditEntryInstruction<
  TProgram extends string = typeof DAILY_JOURNAL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    entry: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: EditEntryInstructionData;
};

export function parseEditEntryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedEditEntryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      entry: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getEditEntryInstructionDataDecoder().decode(instruction.data),
  };
}
